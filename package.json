{
  "name": "monocoop",
  "private": true,
  "dependencies": {
    "commitgpt": "^2.0.0",
    "coop-notificator": "workspace:*",
    "cooparser-ts": "workspace:*",
    "coopback": "workspace:*",
    "coopdoc-generator-ts": "workspace:*",
    "cooptypes": "workspace:*",
    "lerna": "^8.1.6",
    "terminal": "workspace:*"
  },
  "devDependencies": {
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "gpt-commit": "git add . && commitgpt",
    "testnet": "git checkout testnet && git merge dev && lerna publish prerelease && git push && git checkout dev",
    "production": "git checkout testnet && lerna publish --conventional-commits --conventional-graduate && git checkout main && git merge testnet && git push && git checkout dev"
  }
}
