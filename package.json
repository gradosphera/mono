{
  "name": "monocoop",
  "private": true,
  "dependencies": {
    "commitgpt": "^2.0.0",
    "coop-notificator": "workspace:*",
    "cooparser-ts": "workspace:*",
    "coopback": "workspace:*",
    "coopdoc-generator-ts": "workspace:*",
    "cooptypes": "workspace:*",
    "lerna": "^8.1.6",
    "terminal": "workspace:*"
  },
  "devDependencies": {
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "dev:services": "lerna run dev --parallel --scope coopback --scope cooparser-ts --scope coop-notificator --scope cooptypes --scope coopdoc-generator-ts",
    "dev:libs": "lerna run dev --parallel --scope cooptypes",
    "dev:terminal": "lerna run dev --scope terminal",
    "gpt-commit": "git add . && commitgpt",
    "next": "lerna version",
    "testnet": "git checkout testnet && git merge dev && git push && lerna publish prerelease --message 'chore(release): publish' && git checkout dev",
    "production": "git checkout main && git merge testnet && git push && lerna publish --conventional-commits --conventional-graduate --message 'chore(release): publish' && git checkout dev"
  }
}
