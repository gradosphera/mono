{
  "name": "monocoop",
  "private": true,
  "dependencies": {
    "cooparser-ts": "workspace:*",
    "coopdoc-generator-ts": "workspace:*",
    "coop-notificator": "workspace:*",
    "coopback": "workspace:*",
    "cooptypes": "workspace:*",
    "terminal": "workspace:*"
  },
  "devDependencies": {
    "lerna": "^8.1.6"
  },
  "scripts": {
    "publish:testnet": "lerna version --conventional-prerelease --preid testnet && lerna publish from-package --dist-tag testnet",
    "publish:production": "lerna version --conventional-graduate && lerna publish from-package --dist-tag latest"
  }
}
