import type { CapitalContract } from 'cooptypes';

/**
 * Интерфейс данных сегмента из блокчейна
 * Сегмент содержит информацию о вкладах участника в проект
 */
export type ISegmentBlockchainData = CapitalContract.Tables.Segments.ISegment;
