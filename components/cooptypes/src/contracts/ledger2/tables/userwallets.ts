import type * as Ledger2 from '../../../interfaces/ledger2'

export const tableName = 'userwallets'
// scope — coopname (как у wallets/accounts), задаётся при чтении.

/**
 * @interface
 */
export type IUserWallet = Ledger2.IUserWallet
