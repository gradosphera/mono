/**
 * Запрос одного предложения переехал в общий слой сущности
 * (`entities/MarketplaceOffer`): ту же карточку открывают оверлеем реестры
 * стола администратора. Здесь остаётся только реэкспорт, чтобы страница и
 * впредь брала данные из одной точки.
 */
export { fetchOffer } from 'src/entities/MarketplaceOffer';
