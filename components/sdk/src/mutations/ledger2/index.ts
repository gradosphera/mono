/** Перевод между кошельками одного бух.счёта (operation o.adj.walmove). Только председатель. */
export * as WalmoveWallets from './walmoveWallets'

/** Откат ранее проведённой операции зеркальной проводкой (operation o.adj.rev). Только председатель. */
export * as RevertOperation from './revertOperation'
